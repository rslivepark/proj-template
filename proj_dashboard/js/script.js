import './chart.js';
import './event.js';
import 'https://code.jquery.com/jquery-3.7.1.min.js';

/*
function getApi() {
        const config = {
          method: 'get',
        };
        fetch(url, config)
          .then((response) => response.json())
          .then((result) => {
            // Log data inside the function or do something with it
            console.log('result');
            console.log(result.data);
            console.log(result.data[0]['순차번호']);
            console.log(result.data[0]['한글이름']);

            div.textContent = JSON.stringify(result.data[0]['한글이름']);
          })
          .catch((error) => console.error(error));
      }
*/
